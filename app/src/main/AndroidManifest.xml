<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aaronsoftech.in.nber">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWACCESS_FINE_LOCATIONORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_GPS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <activity android:name=".Activity.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.MainActivity"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.login_mobile"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.login_email"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Acc_setting"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Acc_edit"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Trip"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Select_payment"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Payment_add"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Payment_code"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Trip_free"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Password"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Privacy_setting"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Contacts"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Notification"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Help"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Register_mobile"
            android:theme="@style/AppTheme_one" />
        <activity
            android:name=".Activity.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme_one" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <!--
             <meta-data
             android:name="com.google.android.geo.API_KEY"
             android:value="@string/google_maps_key" />
        -->
        <activity
            android:name=".Activity.Social_Login"
            android:theme="@style/AppTheme_one" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activity.MapsActivity"
            android:label="@string/title_activity_maps"                              />
        <activity android:name=".Activity.Verification"      />
        <activity android:name=".Activity.From_Location"     android:theme="@style/AppTheme_one" />
        <activity android:name=".Activity.Driver_document"   android:theme="@style/AppTheme_one" />
        <activity android:name=".Activity.Driver_photo"      android:theme="@style/AppTheme_one" />
        <activity android:name=".Activity.Driver_doc_Image"  android:theme="@style/AppTheme_one" />
        <activity android:name=".Activity.Vehicle_reg"       android:theme="@style/AppTheme_one" ></activity>
    </application>

</manifest>